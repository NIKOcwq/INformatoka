import math

def root(num, r):
    base = num ** (1.0/r)
    roots = [base]
    for i in range(1, r):
        roots.append(complex(base * math.cos(2*math.pi * i / r), base * math.sin(2*math.pi * i / r)))
    return roots
    
    import math

def koren02(a, b):
    base = a ** (1.0/b)
    roots = [base]
    for i in range(1, b):
        roots.append(complex(base * math.cos(2*math.pi * i / b), base * math.sin(2*math.pi * i / b)))
    return koren02
